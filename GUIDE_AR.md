# دليل استخدام وتعديل قالب اللاعب الاحترافي (PlayPro)

هذا الملف يشرح لك بالتفصيل كيف تقوم بتخصيص هذا الموقع لأي لاعب كرة قدم آخر بسهولة، وكيف يعمل نظام الـ QR Code.

## 1. كيفية تغيير بيانات اللاعب
كل بيانات اللاعب موجودة في ملف واحد فقط وسهل الوصول إليه.
قم بفتح الملف التالي: `src/types.ts`

ستجد فيه بيانات تسمى `DUMMY_DATA`، هذه البيانات تحتوي على كل المعلومات التي تراها في الموقع. يمكنك تغييرها لأي لاعب جديد كالتالي:
- `name`: اسم اللاعب (باللغتين الإنجليزية والعربية).
- `number`: رقم اللاعب (يظهر بشكل كبير في الخلفية).
- `position`: اسم مركز اللاعب.
- `club` و `clubLogo`: اسم النادي ولوجو النادي الحالي (ضع رابط الصورة المنتهى بـ png او jpg).
- `socials`: روابط حسابات السوشيال ميديا ورقم الهاتف (لأزرار الاتصال والواتساب).
- `preferredFoot`: القدم المفضلة (أكتب `'right'` لليمنى، أو `'left'` لليسرى).
- `favoriteClub` و `favoriteClubLogo`: النادي المفضل واللوجو الخاص به.
- `rating`: التقييم العام للاعب.
- `heroImage`: الصورة الرئيسية للاعب في الصفحة الأولى.
- `timeline`: قائمة تمثل مسيرة اللاعب (السنوات، أسماء الأندية التي لعب لها، واللوجوهات).

كل ما عليك هو استبدال النصوص والروابط الموجودة داخل علامات التنصيص `''` بالبيانات الجديدة.

## 2. كيفية تغيير نقطة اللاعب على رسمة الملعب التوضيحية
لتغيير مكان نقطة اللاعب على الرسمة البيانية للملعب بناءً على مركزه، لقد قمنا ببرمجة نظام ذكي.
في نفس الملف `src/types.ts`، ستجد حقل يسمى:
`positionCode`

قم بتغيير هذا الكود لأحد الأكواد المتعارف عليها في كرة القدم للمركز المطلوب، وسيتغير مكان النقطة على الملعب تلقائياً بما يتناسب مع هذا الكود:

*   `GK`: حارس مرمى (Goal Keeper)
*   `CB`: قلب دفاع (Center Back)
*   `LB`: ظهير أيسر (Left Back)
*   `RB`: ظهير أيمن (Right Back)
*   `CDM`: خط وسط مدافع (Central Defensive Midfielder)
*   `CM`: خط وسط (Central Midfielder)
*   `CAM`: وسط هجومي صانع ألعاب (Central Attacking Midfielder)
*   `LW`: جناح أيسر (Left Winger)
*   `RW`: جناح أيمن (Right Winger)
*   `ST`: مهاجم صريح (Striker)

## 3. كيف يعمل كود الـ QR الخاص بالموقع (Scan to Connect)؟
منظومة الـ QR Code مصممة لتكون **ديناميكية (تلقائية)** لتسهيل عملية توفير هذا الموقع كمنتج لعدة لاعبين دون التعديل على الأكواد.

*   الموقع يقوم بقراءة الرابط الخاص به (URL) من المتصفح مباشرةً في وقت التشغيل.
*   يقوم بإنشاء الـ QR Code الذي يوجه دائمًا على نفس الرابط الذي تم فتح الموقع منه.
*   **ماذا يعني هذا؟** يعني بمجرد أن تقوم بتشغيل أو استضافة هذا الموقع على دومين خاص باللاعب (مثلاً: `www.omarmuhammed.com`)، فإن الـ QR Code فور الضغط عليه سيقرأ الدومين ويشكل باركود يوجه لهذا الدومين تلقائيا.
*   هذا ممتاز جداً حيث يمكن للاعب فتح هاتفه، إظهار الكود لمدرب أو كشاف، وعند تصوير الشاشة بكاميرا هاتفهم سينتقلون لملفه مباشرة.

## 4. نظام الألوان المتعددة (الثيمات)
لقد أضفنا أيقونة "الباليتة" (الألوان) أعلى الموقع.
نظامنا البرمجي يسمح بتغيير لون الموقع كاملاً (والتأثيرات، والأزرار، ومؤشر الماوس) بضغطة زر ليتناسب مع لون نادي اللاعب المفضل. مدعوم حالياً: (الأخضر، الأزرق، الوردي، البرتقالي، والبنفسجي).
